const initState = {
    isLogin: false,
    token: null,
};
const AuthReducer = (state = initState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};
export default AuthReducer;

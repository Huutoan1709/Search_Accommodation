const initState = {
    userData: {},
};
const MyUserReducer = (state = initState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default MyUserReducer;
